<div class="input-group mb-3">
    <input type="text" disabled class="form-control" placeholder="<%- task.text %>" required>
    <form class="row g-3 needs-validation" action="/claim" method="POST">
        <input name="state" value="<%- task.id %>">
        <input type="hidden" name="username" value="<%= username %>">
        <button type="button" class="btn btn-outline-secondary">Claim</button>
    </form>
</div>
